block icon {

    this.ctx.url, tag: 'img'

    this.ctx.url, attrs: {

        var attrs = {};
        var props = ['width', 'height', 'alt'];

        while (prop = props.shift()) {

            if (this.ctx.hasOwnProperty(prop)) {
                attrs[prop] = this.ctx[prop]
            }
        }
        attrs.src = this.ctx.url;

        return attrs;
    }
}