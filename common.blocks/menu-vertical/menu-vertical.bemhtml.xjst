block('menu-vertical')
(
    elem('item')
    (
        tag()('a'),

        attrs()
        ({
            href: this.ctx.url
        }),

        elemMod('current', true)
        (
            tag()('div'),

            attrs()(undefined)
        )
    )
)