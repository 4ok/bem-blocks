block('menu-vertical')
(
    elem('item')
    (
        match(!this.elemMods.hasOwnProperty('current'))
        (
            def()
            (
                function()
                {
                    var ctx = this.ctx;

                    ctx.block = 'link';
                    ctx.elem  = undefined;
                    ctx.mix   = [
                        { block: this.block, elem: 'item' }
                    ];

                    applyCtx(ctx);
                }
            )
        )
    )
)