block('fotorama')
(
    match(this.ctx.options)
    (
        attrs()
        (
            function()
            {
                var attrs   = {};
                var options = this.ctx.options;

                for (var i in options) {
                    attrs['data-' + i] = options[i];
                }

                return attrs;
            }
        )
    )
);